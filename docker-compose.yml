version: '3'
services:
  dev:
    image: node:10
    volumes:
      - .:/usr/src/service
    working_dir: /usr/src/service
    command: bash -c "npm install -g @angular/cli && npm install -g chrome-webstore-upload-cli && ng serve"
    ports:
        4200
